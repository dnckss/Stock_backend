# Web framework
fastapi==0.135.1
uvicorn[standard]==0.41.0
pydantic==2.12.5
python-dotenv==1.2.2

# HTTP / crawling
httpx==0.28.1
requests==2.32.5
beautifulsoup4==4.14.3
lxml==6.0.2

# Data
pandas==3.0.1
numpy==2.4.2

# Market data
yfinance==1.2.0

# External services
openai==2.26.0
supabase==2.28.3

# FinBERT (감성 분석) — Hugging Face Spaces(16GB RAM) 환경에서 정상 동작.
# Render Free 등 저메모리 환경에서는 두 줄 주석 처리하면 services/finbert.py 가
# 자동으로 neutral 폴백으로 전환된다.
transformers==5.3.0
torch==2.10.0
