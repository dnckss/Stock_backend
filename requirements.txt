# Web framework
fastapi==0.135.1
uvicorn[standard]==0.41.0
pydantic==2.12.5
python-dotenv==1.2.2

# HTTP / crawling
httpx==0.28.1
requests==2.32.5
beautifulsoup4==4.14.3
lxml==6.0.2

# Data
pandas==3.0.1
numpy==2.4.2

# Market data
yfinance==1.2.0

# External services
openai==2.26.0
supabase==2.28.3

# FinBERT (감성 분석) — Render Free Plan(512MB) 메모리 한계로 제외.
# services/finbert.py 가 transformers 미설치를 감지하면 neutral 폴백 반환.
# 유료 플랜 또는 메모리 여유 환경에서는 아래 두 줄 주석 해제하면 자동 복구.
# transformers==5.3.0
# torch==2.10.0
