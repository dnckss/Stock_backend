# Web framework
fastapi==0.135.1
uvicorn[standard]==0.41.0
pydantic==2.12.5
python-dotenv==1.2.2

# HTTP / crawling
httpx==0.28.1
requests==2.32.5
beautifulsoup4==4.14.3
lxml==6.0.2

# Data
pandas==3.0.1
numpy==2.4.2

# Market data
yfinance==1.2.0

# External services
openai==2.26.0
supabase==2.28.3

# FinBERT (감성 분석) — Render Free(512MB) 메모리 한계 회피를 위해 임시 비활성.
# services/finbert.py 가 transformers 미설치 시 OpenAI gpt-4o-mini 폴백으로 동작.
# 메모리 여유 환경(HF Spaces 16GB 등)에서는 아래 두 줄 주석 해제하면 로컬 FinBERT 복귀.
# transformers==5.3.0
# torch==2.10.0
